import 'package:flutter/material.dart';

class AppColors {
  static const Color background = Color(0xFF121212);
  static const Color textPrimary = Color(0xFFFFFFFF);
  static const Color accentGreen = Color(0xFF00FFC2); // Income/Safe
  static const Color accentRed = Color(0xFFFF2E63); // Expense/Debt

  static const Color glassWhite = Color(0x0DFFFFFF); // White with 0.05 opacity
  static const Color glassBorder = Color(
    0x1AFFFFFF,
  ); // White with 0.1 opacity for borders

  static const Color cardBackground = Color(0xFF1E1E1E);
}
